<?xml version="1.0" encoding="UTF-8"?>

<project name="Video" default="help">

  <property name="dir" value="${project.basedir}"/>

  <target name="help" description="lists available targets">
    <exec executable="phing" outputProperty="help">
      <arg value="-l"/>
    </exec>
    <echo>${help}</echo>
  </target>

  <target name="sniff" description="checks adherence to the coding standards">
    <exec executable="phpcs" passthru="true" checkreturn="true">
      <arg value="--standard=PSR12"/>
      <arg line="--runtime-set ignore_warnings_on_exit true"/>
      <arg file="classes"/>
      <arg file="admin.php"/>
      <arg file="index.php"/>
      <arg file="video_view.php"/>
    </exec>
    <exec executable="phpcs" passthru="true" checkreturn="true">
      <arg line="--standard=PSR12 --runtime-set ignore_warnings_on_exit true --exclude=Generic.Files.LineLength config/ languages"/>
    </exec>
  </target>

  <target name="stan" description="runs static analyzer">
    <exec executable="phpstan" passthru="true" checkreturn="true"/>
  </target>

  <target name="test" description="runs all developer tests">
    <exec executable="phpunit" passthru="true" checkreturn="true"/>
  </target>

  <target name="js" description="checks JavaScript">
    <exec executable="prettier" passthru="true" checkreturn="true">
      <arg line="--write --print-width 100 --tab-width 4 --trailing-comma none video.js"/>
    </exec>
    <exec executable="jshint" passthru="true" checkreturn="true">
      <arg line="video.js"/>
    </exec>
    <exec executable="tsc" passthru="true" checkreturn="true">
      <arg line="--checkJS --noEmit --strict --lib es5,dom video.js"/>
    </exec>
  </target>

  <target name="check" description="runs all checks" depends="sniff,stan,test,js"/>

  <target name="coverage" description="generates coverage report">
    <exec executable="phpunit" passthru="true" checkreturn="true">
      <arg line="--coverage-html coverage"/>
    </exec>
  </target>

  <target name="postcss" description="patches CSS for older browsers">
    <exec executable="postcss" passthru="true" checkreturn="true">
      <arg line="${dir}/css -u postcss-preset-env --no-map -r"/>
    </exec>
    <exec executable="prettier" passthru="true" checkreturn="true">
      <arg line="--tab-width 4 ${dir}/css --write"/>
    </exec>
  </target>

  <target name="minify" description="minifies JS">
    <exec executable="uglifyjs" passthru="true" checkreturn="true">
      <arg line="${dir}/video.js --module --mangle --compress -o ${dir}/video.min.js"/>
    </exec>
  </target>

  <target name="gen-help" description="builds help.htm from README.md">
    <exec executable="pandoc" passthru="true" checkreturn="true">
      <arg line="-s --eol=lf --template=help/template.htm -Vlang=en -Vpagetitle=&quot;User Manual&quot; -o help/help.htm README.md -t html"/>
    </exec>
    <exec executable="pandoc" passthru="true" checkreturn="true">
      <arg line="-s --eol=lf --template=help/template.htm -Vlang=de -Vpagetitle=&quot;Benutzerhandbuch&quot; -o help/help_de.htm README_de.md -t html"/>
    </exec>
  </target>

  <target name="build" description="builds a distributable ZIP archive">
    <exec executable="git" checkreturn="true">
      <arg line="archive -o export.zip HEAD"/>
    </exec>
    <unzip file="export.zip" todir="export"/>
    <delete file="export.zip"/>
    <phingcall target="postcss">
      <property name="dir" value="export/"/>
    </phingcall>
    <phingcall target="minify">
      <property name="dir" value="export/"/>
    </phingcall>
    <move todir="dist">
      <fileset dir="export">
        <include name="views/*.php"/>
      </fileset>
      <filterchain>
        <replaceregexp>
          <regexp pattern="&lt;\?(?!=|php)" replace="&lt;?php "/>
        </replaceregexp>
      </filterchain>
    </move>
    <move todir="dist">
      <fileset dir="export">
        <exclude name=".github/**"/>
        <exclude name=".phive/**"/>
        <exclude name="help/template.htm"/>
        <exclude name="itube/**"/>
        <exclude name="tests/**"/>
        <exclude name="README*.md"/>
        <exclude name="build.xml"/>
        <exclude name="composer.*"/>
        <exclude name="phpstan.*"/>
        <exclude name="phpunit.xml"/>
        <exclude name="teplad.xml"/>
      </fileset>
    </move>
    <delete dir="export"/>
    <copy file="dist/config/config.php" tofile="dist/config/defaultconfig.php"/>
    <copy file="dist/languages/en.php" tofile="dist/languages/default.php"/>
    <zip destfile="Video_XH-2.5-dev.zip" basedir="dist" prefix="video/"/>
    <delete dir="dist"/>
  </target>

  <target name="build-patch" description="builds a distributable update package">
    <fail unless="patchee" message="patchee is not defined!"/>
    <unzip file="Video_XH-2.5-dev.zip" todir="current"/>
    <unzip file="${patchee}" todir="patchee"/>
    <copy todir="dist">
      <fileset dir="current">
        <different targetdir="patchee" ignoreFileTimes="true"/>
      </fileset>
    </copy>
    <delete>
      <fileset dir="dist/video">
        <include name="config/config.php"/>
        <include name="languages/??.php"/>
        <include name="css/stylesheet.css"/>
      </fileset>
    </delete>
    <zip destfile="Video_XH-2.5-dev-update-from-2.x.zip" basedir="dist" includeemptydirs="false"/>
    <delete dir="patchee" quiet="true"/>
    <delete dir="current" quiet="true"/>
    <delete dir="dist" quiet="true"/>
  </target>

  <target name="build-itube" description="builds a distributable ITube ZIP archive">
    <exec executable="git" checkreturn="true">
      <arg line="archive -o export.zip HEAD"/>
    </exec>
    <unzip file="export.zip" todir="export"/>
    <delete file="export.zip"/>
    <move todir="dist">
      <fileset dir="export">
        <include name="itube/**"/>
      </fileset>
    </move>
    <delete dir="export"/>
    <zip destfile="ITube-2.5-dev.zip" basedir="dist" prefix=""/>
    <delete dir="dist"/>
  </target>

</project>
